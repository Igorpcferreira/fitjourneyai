package br.edu.puc.fitjourneyai.core.model.enums;

/**
 * Tipo de mensagem no histórico de conversa.
 */
public enum MessageType {

    /** Mensagem enviada pelo usuário. */
    USER,

    /** Mensagem enviada pelo bot. */
    BOT
}
